import {bindings,db} from '@/lib/server';
export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!/^[a-z0-9-]+$/.test(id))return new Response('Not found',{status:404});const exists=await db().prepare('SELECT id FROM listings WHERE id = ?').bind(id).first();if(!exists)return new Response('Not found',{status:404});const obj=await bindings().FILES.get(id);if(!obj)return new Response('Not found',{status:404});return new Response(obj.body,{headers:{'Content-Type':'image/jpeg','Cache-Control':'public, max-age=86400','X-Content-Type-Options':'nosniff'}});}
