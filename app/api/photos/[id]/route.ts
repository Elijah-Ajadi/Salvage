import {admin} from '@/lib/supabase/server';
import {getUser} from '@/lib/auth';
export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){const user=await getUser();if(!user)return new Response('Sign in required',{status:401});const {id}=await params;if(!/^[a-f0-9-]{36}$/.test(id))return new Response('Not found',{status:404});const db=admin();const {data:exists}=await db.from('listings').select('id,status,posted_by,claimed_by').eq('id',id).maybeSingle();if(!exists||(exists.status!=='available'&&exists.posted_by!==user.userId&&exists.claimed_by!==user.userId))return new Response('Not found',{status:404});const {data,error}=await db.storage.from('listing-photos').download(`${id}.jpg`);if(error||!data)return new Response('Not found',{status:404});return new Response(data,{headers:{'Content-Type':'image/jpeg','Cache-Control':'private, max-age=300','X-Content-Type-Options':'nosniff'}});}
