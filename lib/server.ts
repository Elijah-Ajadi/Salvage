import 'server-only';
export function bindings(){return {OPENAI_API_KEY:process.env.OPENAI_API_KEY,OPENAI_VISION_MODEL:process.env.OPENAI_VISION_MODEL,GEMINI_API_KEY:process.env.GEMINI_API_KEY,STRIPE_SECRET_KEY:process.env.STRIPE_SECRET_KEY,STRIPE_PUBLISHABLE_KEY:process.env.STRIPE_PUBLISHABLE_KEY};}
export async function geocode(address:string){const r=await fetch(`https://nominatim.openstreetmap.org/search?format=json&limit=1&q=${encodeURIComponent(address)}`,{headers:{'User-Agent':'Salvage/1.0 (local material exchange)'},signal:AbortSignal.timeout(5000)});if(!r.ok)throw Error('Location lookup is unavailable. Please try again.');const a:any=await r.json();if(!a.length)throw Error('We couldn’t find that address. Include the city and ZIP code.');return {lat:Number(a[0].lat),lng:Number(a[0].lon)};}

